# pi-aruco-cv2-positioning

`python3 tracking.py`


## requirements
* `opencv2-contrib` (python)
* `pip install --user websockets`
* `pip install --user ...`

## extra tipps:
### if ufw used open
`sudo ufw allow 5678`

